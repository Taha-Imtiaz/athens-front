
var initialState = []


var jobReducer = (state = initialState, action) => {
var {type, payload} = action

switch (type) {
    
        

    default:
        return state
}

}
export default jobReducer